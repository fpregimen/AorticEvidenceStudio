"use client";import Link from"next/link";import{useLanguage}from"./language-provider";import{t}from"@/lib/i18n";import{reviewProgress,type ContentReview}from"@/lib/content-review-model";import type{PublicSourceRecord}from"@/lib/source-model";
export function ReviewListClient({items}:{items:Array<{review:ContentReview;source:PublicSourceRecord}>}){const{language}=useLanguage();return <main className="shell py-12"><Link href="/evaluation" className="text-sm font-bold text-blue-700">← {t(language,"Evaluation Framework","評価フレームワーク")}</Link><div className="mt-7 eyebrow">{t(language,"Evaluation Question 2","評価質問2")}</div><h1 className="mt-3 max-w-4xl text-3xl font-semibold">{t(language,"Specialist Validation v2","専門医検証 v2")}</h1><p className="mt-4 max-w-4xl text-lg leading-8 text-slate-700">{items[0]?.review.question}</p><div className="mt-7 space-y-4">{items.map(({review,source})=>{const progress=reviewProgress(review);return <article className="card p-5" key={source.source_id}><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="text-xs font-bold text-blue-700">{source.source_id}</div><h2 className="mt-2 max-w-3xl font-bold">{source.title}</h2><p className="mt-2 text-sm text-slate-600">{progress.completed} / {progress.total} {t(language,"items completed","項目完了")}</p></div><span className={`rounded-full px-3 py-1 text-xs font-bold ${progress.complete?"bg-emerald-100 text-emerald-800":"bg-amber-100 text-amber-900"}`}>{progress.complete?t(language,"Complete","完了"):t(language,"Incomplete","未完了")}</span></div><Link className="btn mt-4 inline-block no-underline" href={`/evaluation/2/review/${source.source_id}`}>{t(language,"Review source","ソースをレビュー")}</Link></article>})}</div><Link href="/evaluation/2/synthesis" className="btn mt-6 inline-block no-underline">{t(language,"Open synthesis validation","統合検証を開く")}</Link></main>}
