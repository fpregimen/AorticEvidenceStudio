"use client";import Link from"next/link";import{useLanguage}from"./language-provider";import{t}from"@/lib/i18n";import type{SynthesisDraft}from"@/lib/synthesis-drafts";
export function SynthesisClient({draft}:{draft:SynthesisDraft}){const{language}=useLanguage();return <main className="shell py-12"><Link className="text-sm font-bold text-blue-700" href="/evaluation/2/review">← {t(language,"Question 2 Review Workspace","質問2レビューワークスペース")}</Link><div className="mt-7 eyebrow">{t(language,"Extraction synthesis draft","抽出統合ドラフト")}</div><h1 className="mt-3 max-w-4xl text-3xl font-semibold">{t(language,"Evaluation Question 2 Synthesis","評価質問2 統合")}</h1><p className="mt-4 max-w-4xl text-lg leading-8">{draft.question}</p><div className="mt-6 rounded-xl border border-amber-300 bg-amber-50 p-4 text-sm font-semibold text-amber-950">{t(language,"Not specialist validated. Not suitable for a generated answer or clinical use.","専門医による検証は未実施です。生成回答や臨床使用には適しません。")}</div><section className="card mt-6 p-6"><h2 className="font-bold">{t(language,"Scope","範囲")}</h2><p className="mt-3 text-sm leading-6 text-slate-700">{draft.scope_note}</p></section><section className="mt-6 space-y-4"><h2 className="text-xl font-bold">{t(language,"Draft findings","ドラフト所見")}</h2>{draft.draft_findings.map((item,index)=><article className="card p-5" key={index}><p className="leading-7">{item.finding}</p><p className="mt-3 text-xs text-slate-500">{t(language,"Claim references","主張参照")}: {item.claim_refs.join(", ")}</p></article>)}</section><section className="card mt-6 p-6"><h2 className="font-bold">{t(language,"Source identity","ソース同一性")}</h2><div className="mt-4 space-y-4">{draft.identity_outcomes.map(item=><div key={item.source_id}><div className="flex flex-wrap items-center gap-2"><b>{item.source_id}</b><span className="badge">{item.outcome}</span></div>{item.warning&&<p className="mt-2 text-sm text-amber-900">{item.warning}</p>}</div>)}</div></section></main>}
