import "server-only";import{readFile}from"node:fs/promises";import path from"node:path";
export interface SynthesisDraft{synthesis_id:string;evaluation_question_number:number;status:string;last_updated:string;question:string;scope_note:string;source_ids:string[];identity_outcomes:Array<{source_id:string;outcome:string;warning:string|null}>;draft_findings:Array<{finding:string;claim_refs:string[]}>;specialist_review_status?:string;synthesis_validation_decision?:"Approved"|"Needs correction";synthesis_reviewer?:string;synthesis_review_date?:string;synthesis_reviewer_note?:string|null;safety:{verified_by_reviewer:boolean;specialist_validated:boolean;suitable_for_generated_answer:boolean}}
export async function getQ02Synthesis(){return JSON.parse(await readFile(path.join(process.cwd(),"database","synthesis_drafts","Q02_PREEMPTIVE_TEVAR_DRAFT.json"),"utf8"))as SynthesisDraft}
