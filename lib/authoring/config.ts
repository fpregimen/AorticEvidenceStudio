export function authoringSetup() { const url=process.env.NEXT_PUBLIC_SUPABASE_URL?.trim(),anonKey=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY?.trim(); return {configured:Boolean(url&&anonKey),url:url??"",anonKey:anonKey??"",bucket:process.env.SUPABASE_PRIVATE_BUCKET?.trim()||"aes-private-sources"}; }
export function authoringAiSetup(){const endpoint=process.env.AUTHORING_AI_API_URL?.trim(),apiKey=process.env.AUTHORING_AI_API_KEY?.trim(),model=process.env.AUTHORING_AI_MODEL?.trim();return{configured:Boolean(endpoint&&apiKey&&model),endpoint:endpoint??"",apiKey:apiKey??"",model:model??"",provider:process.env.AUTHORING_AI_PROVIDER?.trim()||"configured-provider"}}
