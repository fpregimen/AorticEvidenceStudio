import{authoringSetup}from"@/lib/authoring/config";
import{loginAction}from"@/lib/authoring/actions";
export const dynamic="force-dynamic";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const config=authoringSetup(),query=await searchParams;return <main className="shell py-12"><div className="mx-auto max-w-xl"><div className="eyebrow">Private Evidence Authoring Portal / 非公開エビデンス作成ポータル</div><h1 className="mt-3 text-3xl font-semibold">Authorized login / 認証ログイン</h1>{!config.configured?<div className="mt-6 rounded-xl border border-amber-300 bg-amber-50 p-5 text-amber-950"><h2 className="font-bold">Setup required / セットアップが必要です</h2><p className="mt-2 text-sm">Set NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY. The application remains available, but authoring is disabled.</p></div>:<form action={loginAction} className="card mt-6 space-y-4 p-6"><label className="block text-sm font-bold">Email<input required type="email" name="email" className="source-control mt-2" autoComplete="email"/></label><label className="block text-sm font-bold">Password<input required type="password" name="password" className="source-control mt-2" autoComplete="current-password"/></label><button className="btn" type="submit">Log in / ログイン</button>{query.error&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-800">{query.error}</p>}</form>}<p className="mt-5 text-sm text-slate-600">Authorized users only. Do not enter patient-identifiable information. / 許可されたユーザーのみ。患者を特定できる情報は入力しないでください。</p></div></main>}
