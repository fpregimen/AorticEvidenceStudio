import{createSignedPdfUpload}from"@/lib/authoring/payload-safe-pdf";
export async function POST(request:Request){try{const body=await request.json()as{sourceId?:string;sourceVersionId?:string;byteLength?:number;sha256?:string;mimeType?:string};return Response.json(await createSignedPdfUpload({sourceId:String(body.sourceId??""),sourceVersionId:String(body.sourceVersionId??""),byteLength:Number(body.byteLength),sha256:String(body.sha256??""),mimeType:String(body.mimeType??"")}))}catch(error){return Response.json({error:error instanceof Error?error.message:"Unable to authorize upload"},{status:400})}}
